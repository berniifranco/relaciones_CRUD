<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= actor.first_name %> <%= actor.last_name %> </title>
    <link rel='stylesheet' href='/css/style.css' />

</head>
<body>
    <figure>
        <img src="/img/logo-DH.png" alt="Logo Digital House">
    </figure>
    <h1><%= actor.first_name %> <%= actor.last_name %></h1>    
    <br>
    <p>RATING: <%= actor.rating %> </p>
    <p>PELICULA FAVORITA: <%= actor.peliculas_favoritas.title %> </p>
    <br>
    <section>
        <!--Aquí debe colocar los botones de Modificar - Borrar y Listado de Películas-->
        <a href="/actors/edit/<%= actor.id %>"><button type="submit">Editar</button></a>
        <form action="/actors/delete/<%= actor.id %>" method="GET">
            <button type="submit">Borrar</button>
        </form>
    </section>

</body>
</html>